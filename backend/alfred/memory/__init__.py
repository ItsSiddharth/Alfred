"""
memory package — ALFRED's persistent memory engine.

Public modules:
  store    — CRUD over MemoryItem rows
  compress — LLM compression to compact Markdown
  context  — token-budgeted context injection
"""