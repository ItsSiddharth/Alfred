{
  "name": "alfred-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python scripts/dev.py",
    "frontend": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}